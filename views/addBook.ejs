<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Добавить книгу — BookShop</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body data-role="<%= user ? user.role : '' %>">
<%- include('partials/header') %>

<main class="container">
    <h1>Добавить книгу</h1>

    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <form action="/books/add" method="post" enctype="multipart/form-data" class="add-form">
        <div class="form-group">
            <label for="title">Название</label><br>
            <input id="title" type="text" name="title" placeholder="Название книги" required>
        </div>

        <div class="form-group">
            <label for="author">Автор</label><br>
            <input id="author" type="text" name="author" placeholder="Автор книги" required>
        </div>

        <div class="form-group">
            <label for="price">Цена, ₽</label><br>
            <input id="price" type="number" step="0.01" name="price" placeholder="Цена, например 499.99" required>
        </div>

        <div class="form-group">
            <label for="description">Описание</label><br>
            <textarea id="description" name="description" placeholder="Краткое описание"></textarea>
        </div>

        <div class="form-group">
            <label for="image">Обложка книги</label><br>
            <input id="image" type="file" name="image" accept="image/jpeg,image/png">
        </div>

        <button type="submit" class="btn btn-primary">Добавить книгу</button>
    </form>

    <p style="margin-top:20px;">
        <a href="/books" class="btn btn-link">← Назад к каталогу</a>
    </p>
</main>
</body>
</html>
