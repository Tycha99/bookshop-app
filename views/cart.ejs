<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Корзина</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="menu">
    <% if (user) { %>
        Вы вошли как: <b><%= user.username %></b> (<%= user.role %>) |
        <a href="/logout">Выйти</a>
        | <a href="/">Главная</a>
        | <a href="/books">Каталог</a>
        <% if (user.role === 'buyer') { %>
            | <a href="/cart">Корзина</a>
            | <a href="/orders">Мои заказы</a>
        <% } %>
        <% if (user.role === 'admin') { %>
            | <a href="/orders/all">Все заказы</a>
        <% } %>

<% } else { %>
        <a href="/login">Вход</a> |
        <a href="/register">Регистрация</a> |
        <a href="/books">Каталог</a>
    <% } %>
</div>
<h1>Ваша корзина</h1>
<% if (books && books.length > 0) { %>
    <ul>
        <% books.forEach(function(book) { %>
            <li>
                <strong><%= book.title %></strong> — <%= book.author %> — <%= book.price %> руб. × <%= book.quantity %>
                <form action="/cart/remove" method="post" style="display:inline;">
                    <input type="hidden" name="bookId" value="<%= book.id %>">
                    <button type="submit" style="background: #e74c3c;">Удалить</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <form action="/cart/checkout" method="post">
        <button type="submit">Оформить заказ</button>
    </form>
<% } else { %>
    <p>Корзина пуста.</p>
<% } %>
<a href="/books">Продолжить покупки</a>
</body>
</html>
